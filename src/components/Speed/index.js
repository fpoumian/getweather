import React from 'react'
import PropTypes from 'prop-types'

const Speed = (props) => {
  return (
    <div>Speed component</div>
  )
}

Speed.propTypes = {}
Speed.defaultProps = {}

export default Speed