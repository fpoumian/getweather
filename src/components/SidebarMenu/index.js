import React from 'react'
import PropTypes from 'prop-types'

const SidebarMenu = (props) => {
  return (
    <div>SidebarMenu component</div>
  )
}

SidebarMenu.propTypes = {}
SidebarMenu.defaultProps = {}

export default SidebarMenu